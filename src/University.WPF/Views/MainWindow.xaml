<Window x:Class="University.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:University.WPF"
        xmlns:vm="clr-namespace:University.WPF.ViewModels"
        mc:Ignorable="d"
        Title="University" Height="450" Width="800"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
        FontSize="18">
    
    <Grid ShowGridLines="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!-- TreeView -->
        
        <TreeView Grid.Row="0"
                  Grid.Column="0"
                  ItemsSource="{Binding CourseViewModels}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding GroupsByCourseViews}">
                    <TextBlock Text="{Binding Name}"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
            <!-- <TreeView.ItemTemplate> -->
            <!--     <HierarchicalDataTemplate ItemsSource="{Binding GroupsByCourseViews}" DataType="{x:Type vm:CourseViewModel}"> -->
            <!--         <TextBlock Text="{Binding Name}"/> -->
            <!--         <HierarchicalDataTemplate.ItemTemplate> -->
            <!--             <DataTemplate DataType="{x:Type vm:GroupViewModel}"> -->
            <!--                 <TextBlock Text="{Binding Name}"/> -->
            <!--             </DataTemplate> -->
            <!--         </HierarchicalDataTemplate.ItemTemplate> -->
            <!--     </HierarchicalDataTemplate> -->
            <!-- </TreeView.ItemTemplate> -->
        </TreeView>
        
        <DataGrid Grid.Row="0"
                  Grid.Column="1"
                  ItemsSource="{Binding CoursesView}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False">
            
            <DataGrid.Columns>
                <DataGridTextColumn Width="Auto" 
                                    Header="Id" 
                                    Binding="{Binding Id}"/>
                <DataGridTextColumn Width="*" 
                                    Header="Course" 
                                    Binding="{Binding Name}"/>
            </DataGrid.Columns>
            
        </DataGrid>
    </Grid>
</Window>
